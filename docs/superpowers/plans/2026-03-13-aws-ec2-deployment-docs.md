# AWS EC2 Deployment Documentation Implementation Plan

> **For agentic workers:** REQUIRED: Use superpowers:subagent-driven-development (if subagents available) or superpowers:executing-plans to implement this plan. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add a complete AWS EC2 (Free Tier) deployment section to `README.md`, covering everything from EC2 launch to updates and monitoring.

**Architecture:** Documentation-only change — update `README.md` with a new "Deploying to AWS EC2 (Free Tier)" section modelled after the existing Render section. Also update `docker-compose.yml` to use `image:` instead of `build:` to reflect the Docker Hub–based deployment.

**Tech Stack:** Markdown, Docker, AWS EC2 (Amazon Linux 2023), Docker Hub

---

## Chunk 1: Update docker-compose.yml and README.md

### Task 1: Update docker-compose.yml to use Docker Hub image

**Files:**
- Modify: `docker-compose.yml`

- [ ] **Step 1: Replace `build:` with `image:` in docker-compose.yml**

The current `docker-compose.yml` uses `build: .` which requires buildx. Production deployments pull from Docker Hub instead.

Change:
```yaml
services:
  bot:
    build: .
    restart: unless-stopped
    env_file: .env
    volumes:
      - ./session.session:/app/session.session
```

To:
```yaml
services:
  bot:
    image: ${DOCKER_IMAGE:-tele-bot}
    restart: unless-stopped
    env_file: .env
    volumes:
      - ./session.session:/app/session.session
```

> This keeps local `docker compose up` working (build manually first) and lets EC2 pull from Docker Hub by setting `DOCKER_IMAGE=elgnas2512/tele-bot:latest` in `.env`.

- [ ] **Step 2: Verify local compose still works**

```bash
docker build -t tele-bot .
docker compose up -d
docker compose logs -f bot
```

Expected: bot starts, no errors.

- [ ] **Step 3: Commit**

```bash
git add docker-compose.yml
git commit -m "feat: support Docker Hub image in docker-compose via DOCKER_IMAGE env var"
```

---

### Task 2: Add AWS EC2 deployment section to README.md

**Files:**
- Modify: `README.md`

- [ ] **Step 1: Add "Deploying to AWS EC2 (Free Tier)" section after the Render section**

Insert the following section into `README.md` after the `## Deploying to Render` section:

````markdown
## Deploying to AWS EC2 (Free Tier)

The bot runs as a Docker container on a t2.micro EC2 instance. The image is built locally and pushed to Docker Hub — EC2 only pulls and runs it, requiring no build tools on the server.

**Free tier coverage:** t2.micro gives 750 hours/month free for 12 months — enough for one instance running 24/7. After 12 months, t2.micro costs ~$8–10/month.

### Prerequisites

- AWS account (free tier)
- Docker Hub account (free) — [hub.docker.com](https://hub.docker.com)
- Docker installed locally
- A valid `session.session` file (generated by running the bot locally at least once)

---

### Step 1 — Build and push the Docker image (local machine)

```bash
docker login
docker build -t <your-dockerhub-username>/tele-bot:latest .
docker push <your-dockerhub-username>/tele-bot:latest
```

---

### Step 2 — Launch an EC2 instance

1. Go to **EC2 Console → Launch Instance**
2. **AMI:** Amazon Linux 2023 (kernel-6.1) — free tier eligible
3. **Instance type:** t2.micro
4. **Key pair:** create or select an existing key pair — download the `.pem` file
5. **Security group:** allow SSH (port 22) from your IP only — no inbound ports needed for the bot
6. **Storage:** 8 GB gp2 (default)
7. **Elastic IP (recommended):** after launch, allocate an Elastic IP and associate it with the instance so the public IP doesn't change on stop/start

---

### Step 3 — SSH into the instance

```bash
chmod 400 my-key.pem
ssh -i my-key.pem ec2-user@<your-ec2-public-ip>
```

The default username on Amazon Linux 2023 is `ec2-user`.

---

### Step 4 — Install Docker on EC2

```bash
sudo dnf install -y docker
sudo systemctl enable --now docker
sudo usermod -aG docker ec2-user
newgrp docker
```

Install the Docker Compose plugin:

```bash
sudo dnf install -y docker-compose-plugin
```

If `docker-compose-plugin` is not available in the AL2023 repos, install manually:

```bash
sudo mkdir -p /usr/local/lib/docker/cli-plugins
sudo curl -SL https://github.com/docker/compose/releases/latest/download/docker-compose-linux-x86_64 \
  -o /usr/local/lib/docker/cli-plugins/docker-compose
sudo chmod +x /usr/local/lib/docker/cli-plugins/docker-compose
docker compose version
```

---

### Step 5 — Clone the repo and configure

```bash
git clone <your-repo-url>
cd telegram-intel-bot
```

Create the `.env` file:

```bash
nano .env
```

Add the following (see Configuration section for all variables):

```ini
TELEGRAM_API_ID=your_api_id
TELEGRAM_API_HASH=your_api_hash
BOT_TOKEN=your_bot_token
GEMINI_API_KEY=your_gemini_api_key
CHANNELS=channel1,channel2
DATABASE_URL=your_aiven_postgres_url
DOCKER_IMAGE=<your-dockerhub-username>/tele-bot:latest
```

---

### Step 6 — Copy the Telethon session file

From your **local machine**, copy the `session.session` file to EC2:

```bash
scp -i my-key.pem session.session ec2-user@<your-ec2-public-ip>:~/telegram-intel-bot/
```

---

### Step 7 — Start the bot

```bash
docker compose pull
docker compose up -d
```

Verify it's running:

```bash
docker compose logs -f bot
docker ps
```

The `restart: unless-stopped` policy ensures the container restarts automatically on crash or EC2 reboot (Docker service is enabled via systemd in Step 4).

---

### Updating the bot

When you push code changes:

**Local machine:**
```bash
docker build -t <your-dockerhub-username>/tele-bot:latest .
docker push <your-dockerhub-username>/tele-bot:latest
```

**EC2:**
```bash
docker compose pull
docker compose up -d
```

---

### Monitoring and logs

**View bot logs:**
```bash
docker compose logs -f bot
```

**View EC2 resource usage:**
```bash
top
df -h
```

**AWS Console:** Go to EC2 → select instance → **Monitoring** tab for CPU, network, and disk metrics via CloudWatch.

**Billing alerts:** Go to **Billing → Budgets → Create budget** and choose **Zero spend budget** to get an email if any charges occur. Highly recommended as the free tier expires after 12 months.
````

- [ ] **Step 2: Verify README renders correctly**

Open `README.md` in a markdown previewer or review it locally. Check:
- All code blocks are properly closed
- Section headings are consistent with existing README style
- No broken links

- [ ] **Step 3: Commit**

```bash
git add README.md
git commit -m "docs: add AWS EC2 free tier deployment guide"
```
